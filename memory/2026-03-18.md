- Added Accessories menu + TPMS pages. TPMS lookup uses KM inventory endpoint with vendor fallback due to KM 500 bug when VendorName omitted.
- Added TPMS product page with price = cost + $45 and Hamaton-only enrichment (image/title/bullets/torque + source link). Removed supplier info from customer UI/API (only exposed in debug).
