# 2026-03-16

## Warehouse Tire site
- Fixed prod crash on `/tires/c/all-terrain` (SSR fetch base URL + correct `searchParams` typing). Deployed to Vercel.
- Diagnosed All-Terrain category returning no results: `/api/wp/tires/browse` was unintentionally applying `priceMin=0` and `priceMax=0` when params were missing, filtering out everything.
- Fixed numeric query param parsing so missing/blank `priceMin`/`priceMax` become `null` (no price filter).
- Added debug=2 diagnostics to `/api/wp/tires/browse` temporarily to inspect generated SQL + bound values.
- Confirmed `/tires/c/all-terrain` loads and results now display.
- Added brand + rim dropdown facets with exact matching and counts:
  - new endpoint `/api/wp/tires/facets`
  - page `/tires/c/all-terrain` now uses `<select>` for Brand and Rim.
- Fixed filters not applying due to caching by setting `export const dynamic = "force-dynamic";` on `/tires/c/all-terrain`.
- Implemented homepage 18"/20" wheel shortcuts that require vehicle selection (YMM modal) and carry diameter through.
- Wheels fixes:
  - normalize diameter/width params and compare numeric values (20 vs 20.0) so size filters work.
  - add fallback query when strict fitment yields 0.
  - for size-only searches, avoid upstream fitment constraints and rely on client-side filtering.
- Next likely major feature: support OEM staggered fitments (front/rear specs) as first-class packages.
- Investigated CustomWheelOffset for the requested 2009 Mustang Shelby GT500KR example; found direct vehicle page https://www.customwheeloffset.com/2009-ford-mustang and wheels browse https://www.customwheeloffset.com/store/wheels/2009-ford-mustang showing vehicle context (bolt pattern 5x114.3) and big diameter/width/offset facets.
- UX pattern desired: persistent left-rail “Recommended Fitment” module (shows for any selected vehicle; square or staggered), plus larger product tiles. Staggered flow: wheel PDP can be a dedicated “Staggered” page (Front + Rear wheel listed) and the “Add Tires” CTA carries front/rear wheel IDs via query params (`swf` / `swr`) into tire browsing.
- Verified homepage YMM → vehicle hub → vehicle-scoped browse pattern via screenshots:
  - Vehicle hub page: `/2010-ford-mustang` (category tile grid + “Change Vehicle”)
  - Wheels browse: `/store/wheels/2010-ford-mustang` where sidebar includes AI filter box, delivery ZIP, and Search-by-Vehicle panel with Recommended Fitment (informational) above facets.

## 2026-03-16 late session (CustomWheelOffset → WTD UI work)
- Implemented informational "Recommended Fitment" card in WTD and wired it into:
  - /wheels (left sidebar)
  - /tires (left sidebar)
  - /wheels/[sku] (PDP right rail)
  - /tires/[sku] (PDP right rail)
- Pushed commits:
  - 503c0ad Add informational recommended fitment card to wheels sidebar
  - 299c8f7 Show recommended fitment card on tires and PDPs
- Next chosen by Scott: refactor tire category pages (A) to use same sidebar layout as /tires.
- Completed: category pages now have sidebar + fitment and preserve vehicle params; commit 2c770b9.
- Next: show a "Search by Vehicle" module on category pages even when no vehicle is selected, including a mobile-friendly vehicle bar.

## Bookmark / where we left off (for next session)
- Continue: "CustomWheelOffset-up" styling passes are now in good shape (wide layout, chunky filters, big cards, badges, red brand accents).
- Latest pushes:
  - cadeae1: red brand accents (CTAs, badge highlights, hover)
  - 5f391e4: thin red accent bars (top + left) on cards, fitment module red header, subtle hero gradient
  - 8223176: softened red accents (outline-style badges + lighter red bars)
- Next suggested improvements (tomorrow):
  - Add the same accent bars/treatment to **other card components** (e.g., RecommendedFitmentCard inner panels, any quote tiles) for consistency.
  - Consider adding **review stars** + review count placeholders on cards (UI-only first), similar to CustomWheelOffset.
  - Optionally add a small "delivery / ETA" line under price to mimic CustomWheelOffset.
  - Start scoping the real big feature: **vehicle hub page** after YMM selection + staggered fitment packages (front/rear) workflow.
- Reference URLs from CWO:
  - Vehicle hub: https://www.customwheeloffset.com/2010-ford-mustang
  - Wheels browse: https://www.customwheeloffset.com/store/wheels/2010-ford-mustang
- Reference screenshots saved in workspace root:
  - `second page.jpg` (vehicle hub)
  - `page 3.jpg` (wheels browse w/ recommended fitment sidebar)

## TODO / waiting
- FitmentGuide.com: waiting to hear back; may provide a cleaner solution for front/rear fitment ranges & staggered logic (replace current heuristic where possible).

## Next UX work
- "CustomOffsets-up" search results pages:
  - Use more screen width (reduce side gutters; consider max-w-7xl/max-w-screen-2xl).
  - Larger, bolder product cards (bigger image area; clearer spec/price).
  - Sidebar typography/spacing improved (easier to scan; larger controls).
  - Add badge row + color accents so the page is more eye-catching (while keeping it clean).

## 2026-03-16 (~20:15-20:45) additional progress
- CustomOffsets-up browse pages:
  - Widened /wheels + /tires containers to max-w-screen-2xl.
  - Sidebar widened to 340px.
  - Results grid is now 4-up on xl.
  - Wheel cards: larger imagery + bolder typography/CTA.
  - Sidebar filters: “tall and chunky” controls (h-12, text-base, bigger spacing).
  - Tire cards: updated to match wheel-card boldness (bigger image, pills, CTA).
  - Added badge row on wheel + tire cards (Fast shipping / Fitment checked / + contextual).
- Commits: 0e0b3a1, 377b875, 9ea54cf, e633057, cadeae1, 5f391e4, 8223176

## Deploy notes
- Vercel production was previously pinned to an older commit; pushing latest commits triggered correct deployment.
- Key commits (main):
  - f0a140d derive base URL from request headers for SSR.
  - d3d6f53 fix browse priceMin/priceMax parsing.
  - f52dd4a add facets endpoint + dropdowns.
  - 56b7507 force dynamic render so query filters apply.
