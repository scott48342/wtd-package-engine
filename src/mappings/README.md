# mappings/

Supplier-specific mappings and normalization helpers.

Examples:
- mapping supplier SKU namespaces to internal product IDs
- mapping supplier brand codes to canonical brand names
- translating supplier-specific enums (inventory types, price types)

Keep these separate so adapter implementations stay small.
